let filters = {}

/** 格式化时间
 *  @param {string} time 需要格式化的时间
 */
filters.dateFormat = (time) => {
  console.log(time)
  if (!time) return ''
  return time.substring(0,4) + '-' + time.substring(4,6) + '-' + time.substring(6,8)
};


module.exports = filters;
